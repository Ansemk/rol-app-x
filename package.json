{
  "name": "rol-app-x",
  "version": "0.0.0",
  "description": "Rol application x",
  "main": "index.js",
  "repository": "https://github.com/Ansemk/rol-app-x.git",
  "author": "David Cañamares",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "story": "lerna run story --stream",
    "prestart": "lerna run libbuild --stream",
    "start": "lerna run start --stream"
  }
}
